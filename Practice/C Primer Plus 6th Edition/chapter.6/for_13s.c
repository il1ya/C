// считать двойками, десятками
#include <stdio.h>
int main (void)
{
	int n; // счет с интервалом 13, начиная с 2
	for (n = 2; 2 < 60; n = n + 13) // значение переменной n на каждой интерации увеличивается на 13,
		printf("%d\r\n", n);
	return 0;
}