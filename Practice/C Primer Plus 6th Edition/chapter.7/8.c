//Что выведет следующая программа?
#include <stdio.h>
int main(void)
{
	int age = 20;
	while(age++ <= 65)
	{
		if((age % 20) == 0)
			printf("%d\r\n", age);
		if(age = 65)
			printf("%d\r\n", age);
	}
	return 0;
}
/*
ксожалению, она бесконечно выводит одну и ту ж е строку: Вам 65. Получите свои золотые часы. Проблема в том, что строка if (age = 65)
устанавливает переменную age в 65, что на каждой итерации цикла интерпретируется как истинное.
*/  